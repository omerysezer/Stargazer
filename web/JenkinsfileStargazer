pipeline {
	agent any

	stages{
		stage("Run Dockerfile"){
			steps{
			    sh("docker compose -f web/docker-compose-stargazer.yml up --force-recreate --build -d")
			}
		}
	}
	post{
	    always{
	        cleanWs()
	    }
	}
}
